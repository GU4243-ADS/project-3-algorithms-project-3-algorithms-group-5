---
title: "evaluation"
author: "sile yang"
date: "4/7/2018"
output: html_document
---

```{r message=FALSE, warning=FALSE}
#require packages 

packages <- c("MLmetrics","Metrics","tidyverse")

packages <- lapply(packages, FUN = function(x) {
  if(!require(x, character.only = TRUE)) {
    install.packages(x)
  library(x, character.only = TRUE)
  }
})
```

```{r message=FALSE, warning=FALSE}
# Step 1: load your pred and test data
load("/Users/miezai/Documents/GitHub/project-3-algorithms-project-3-algorithms-group-5/data/EM_pred_entropy.RData")
load("/Users/miezai/Documents/GitHub/project-3-algorithms-project-3-algorithms-group-5/data/EM_UI_test.RData")
load("/Users/miezai/Documents/GitHub/project-3-algorithms-project-3-algorithms-group-5/data/movie_pred.RData")

pred = movie_pred
test = movie_UI_test

pred_MS = MS_pred
test_MS = MS_test

source("../lib/evaluation.R")
```


### Evaluation 
```{r echo=FALSE, message=FALSE, warning=FALSE}
#MAE for EM
eva_mae(pred, test)
```

```{r message=FALSE, warning=FALSE}
#RMSE for EM
eva_rmse(pred, test)
```

```{r message=FALSE, warning=FALSE}
#Rank scoring for MS
rank_scoring(pred_MS, test_MS, 5)
```

